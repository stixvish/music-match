pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.py"
      run: uv run ruff format --check {staged_files}
    lint:
      glob: "*.py"
      run: uv run ruff check {staged_files}
    types:
      glob: "*.py"
      run: uv run mypy src
    tests:
      # the suite runs in under a second; a commit with failing tests reached
      # the remote once because nothing checked here.
      glob: "*.py"
      run: uv run pytest -q -m "not live"
    secrets:
      run: gitleaks protect --staged --no-banner
    spec:
      glob: "SPEC.md"
      run: uv run python tools/check_spec.py

commit-msg:
  commands:
    conventional:
      # SPEC.md rule: conventional, lowercase, under 70 chars, no body
      run: |
        head -1 {1} | grep -qE '^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\([a-z-]+\))?: .{1,60}$' \
          || { echo "bad subject: conventional, lowercase, <70 chars"; exit 1; }
